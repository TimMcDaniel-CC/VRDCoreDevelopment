using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[AddComponentMenu("VRD/Interactors/Grab Interactor (Trigger Based)")]
public class GrabInteractorTrigger : BaseInteractor
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
